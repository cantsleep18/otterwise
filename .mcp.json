{
  "mcpServers": {
    "python-repl": {
      "command": "bash",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/start.sh"],
      "env": {
        "OTTERWISE_VENV": "${CLAUDE_PLUGIN_DATA}/.venv",
        "PYTHONUNBUFFERED": "1"
      }
    }
  }
}
