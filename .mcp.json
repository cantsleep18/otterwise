{
  "mcpServers": {
    "python-repl": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/dist/index.js"],
      "env": {
        "OTTERWISE_VENV": "${CLAUDE_PLUGIN_DATA}/.venv",
        "NODE_ENV": "production"
      }
    }
  }
}
