{
  "hooks": [
    {
      "name": "validate-finding",
      "description": "Verify that each research agent produces valid finding-*.md files with required YAML frontmatter after completing analysis",
      "event": "afterToolResult",
      "command": "bash $PLUGIN_DIR/scripts/validate-finding.sh $TOOL_INPUT",
      "condition": {
        "tool": "TaskUpdate",
        "matchArgs": {
          "status": "completed"
        }
      }
    }
  ]
}
